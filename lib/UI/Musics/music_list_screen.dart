import 'package:flutter/material.dart';

class MusicListScrenn extends StatelessWidget {
  const MusicListScrenn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Musics Screen"),
      ),
    );
  }
}
